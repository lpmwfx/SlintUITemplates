# src/layout/named_parser.rs

## `pub fn parse_named(dsl: &str) -> Vec<NamedPanel>`

*Line 7 · fn*

Parse DSL v2 string into a flat list of named panels.
Horizontal split at top level; vertical split when "/" separates rows.
N am ed p ar se r.

---

