/// T ab le co lu mn struct.
/// struct `TableColumn`.
/// struct `TableColumn`.
/// struct `TableColumn`.
/// struct `TableColumn`.
/// struct `TableColumn`.
/// struct `TableColumn`.
/// struct `TableColumn`.
export struct TableColumn {
    key: string,
    label: string,
    width: length,
}

/// T ab le ro w struct.
/// struct `TableRow`.
/// struct `TableRow`.
/// struct `TableRow`.
/// struct `TableRow`.
/// struct `TableRow`.
/// struct `TableRow`.
/// struct `TableRow`.
export struct TableRow {
    id: string,
    cells: [string],
}
