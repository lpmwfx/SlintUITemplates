/// M ul ti se le ct op ti on struct.
/// struct `MultiSelectOption`.
/// struct `MultiSelectOption`.
/// struct `MultiSelectOption`.
/// struct `MultiSelectOption`.
/// struct `MultiSelectOption`.
/// struct `MultiSelectOption`.
/// struct `MultiSelectOption`.
export struct MultiSelectOption {
    label: string,
    value: string,
}
