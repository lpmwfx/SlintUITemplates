import { Spacing } from "../globals/theme.slint";
import { AccordionRow, AccordionItem } from "accordion-row.slint";

export { AccordionItem }

/// A cc or di on component.
/// component `Accordion`.
/// component `Accordion`.
/// component `Accordion`.
/// component `Accordion`.
/// component `Accordion`.
/// component `Accordion`.
/// component `Accordion`.
export component Accordion inherits VerticalLayout {
    /// Input property "items".
    /// property `items`.
    /// property `items`.
    /// property `items`.
    /// property `items`.
    /// property `items`.
    /// property `items`.
    /// property `items`.
    in property <[AccordionItem]> items: [];
    /// Input property "expanded-id".
    /// property `expanded-id`.
    /// property `expanded-id`.
    /// property `expanded-id`.
    /// property `expanded-id`.
    /// property `expanded-id`.
    /// property `expanded-id`.
    /// property `expanded-id`.
    in property <string>          expanded-id: "";
    /// Callback fired for t og gl ed.
    /// callback `toggled`.
    /// callback `toggled`.
    /// callback `toggled`.
    /// callback `toggled`.
    /// callback `toggled`.
    /// callback `toggled`.
    /// callback `toggled`.
    callback toggled(string);

    spacing: Spacing.xs;

    for item in root.items: AccordionRow {
        item: item;
        expanded: item.id == root.expanded-id;
        toggled(id) => { root.toggled(id); }
    }
}
