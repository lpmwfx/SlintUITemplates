// Popover — inline overlay that appears below the trigger element.
//
// Usage:
//   popover-wrap := Rectangle {
//       Popover {
//           open: show-popover;
//           title: "Info";
//           body: "This is a popover.";
//           close-requested => { show-popover = false; }
//       }
//       Button { clicked => { show-popover = !show-popover; } }
//   }

import { Spacing } from "../globals/theme.slint";
import { Sizes } from "../globals/theme/sizes.slint";
import { PopoverInner } from "popover-inner.slint";

/// P op ov er component.
/// component `Popover`.
/// component `Popover`.
/// component `Popover`.
/// component `Popover`.
/// component `Popover`.
/// component `Popover`.
/// component `Popover`.
export component Popover inherits Rectangle {
    /// Input property "open".
    /// property `open`.
    /// property `open`.
    /// property `open`.
    /// property `open`.
    /// property `open`.
    /// property `open`.
    /// property `open`.
    in property <bool>   open: false;
    /// Input property "popup-width".
    /// property `popup-width`.
    /// property `popup-width`.
    /// property `popup-width`.
    /// property `popup-width`.
    /// property `popup-width`.
    /// property `popup-width`.
    /// property `popup-width`.
    in property <length> popup-width:  Sizes.panel-md;
    /// Input property "popup-height".
    /// property `popup-height`.
    /// property `popup-height`.
    /// property `popup-height`.
    /// property `popup-height`.
    /// property `popup-height`.
    /// property `popup-height`.
    /// property `popup-height`.
    in property <length> popup-height: Sizes.comp-140;
    /// Input property "title".
    /// property `title`.
    /// property `title`.
    /// property `title`.
    /// property `title`.
    /// property `title`.
    /// property `title`.
    /// property `title`.
    in property <string> title: "";
    /// Input property "body".
    /// property `body`.
    /// property `body`.
    /// property `body`.
    /// property `body`.
    /// property `body`.
    /// property `body`.
    /// property `body`.
    in property <string> body: "";
    /// Callback fired for c lo se r eq ue st ed.
    /// callback `close-requested`.
    /// callback `close-requested`.
    /// callback `close-requested`.
    /// callback `close-requested`.
    /// callback `close-requested`.
    /// callback `close-requested`.
    /// callback `close-requested`.
    callback close-requested();

    clip: false;

    @children

    if root.open: PopoverInner {
        y: parent.height + Spacing.xs;
        width: root.popup-width;
        height: root.popup-height;
        title: root.title;
        body: root.body;
        close-requested => { root.close-requested(); }
    }
}
