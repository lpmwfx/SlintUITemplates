import { Colors, Type } from "../../globals/theme.slint";
import { Sizes } from "../../globals/theme/sizes.slint";

/// S pi nv al ue di sp la y component.
/// component `SpinValueDisplay`.
/// component `SpinValueDisplay`.
/// component `SpinValueDisplay`.
/// component `SpinValueDisplay`.
/// component `SpinValueDisplay`.
/// component `SpinValueDisplay`.
/// component `SpinValueDisplay`.
export component SpinValueDisplay inherits Rectangle {
    /// Input property "display-value".
    /// property `display-value`.
    /// property `display-value`.
    /// property `display-value`.
    /// property `display-value`.
    /// property `display-value`.
    /// property `display-value`.
    /// property `display-value`.
    in property <int> display-value;

    horizontal-stretch: Sizes.one;

    Text {
        text: root.display-value;
        font-size: Type.body-size;
        color: Colors.text-primary;
        horizontal-alignment: center;
        vertical-alignment: center;
    }
}
