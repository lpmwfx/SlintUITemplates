/// T ab le co lu mn struct.
/// struct `TableColumn`.
export struct TableColumn {
    key: string,
    label: string,
    width: length,
}

/// T ab le ro w struct.
/// struct `TableRow`.
export struct TableRow {
    id: string,
    cells: [string],
}
