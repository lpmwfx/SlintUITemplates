/// T ab le co lu mn struct.
export struct TableColumn {
    key: string,
    label: string,
    width: length,
}

/// T ab le ro w struct.
export struct TableRow {
    id: string,
    cells: [string],
}
