// Shell type definitions — shared by AppShell, DesktopShell, MobileShell.

import { NavItem } from "../desktop/types.slint";
export { NavItem }

export struct ShellMenuItem {
    id:    string,
    label: string,
}

export struct ShellMenu {
    id:    string,
    label: string,
}

export struct ShellToolbarItem {
    id:      string,
    icon:    string,
    tooltip: string,
}
