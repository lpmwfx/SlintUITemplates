import { Colors, Spacing, Type } from "../../globals/theme.slint";
import { Sizes } from "../../globals/theme/sizes.slint";

/// S cr ol ld em or ow component.
/// component `ScrollDemoRow`.
/// component `ScrollDemoRow`.
/// component `ScrollDemoRow`.
/// component `ScrollDemoRow`.
/// component `ScrollDemoRow`.
/// component `ScrollDemoRow`.
/// component `ScrollDemoRow`.
export component ScrollDemoRow inherits Rectangle {
    /// Input property "row-index".
    /// property `row-index`.
    /// property `row-index`.
    /// property `row-index`.
    /// property `row-index`.
    /// property `row-index`.
    /// property `row-index`.
    /// property `row-index`.
    in property <int>    row-index;
    /// Input property "row-prefix".
    /// property `row-prefix`.
    /// property `row-prefix`.
    /// property `row-prefix`.
    /// property `row-prefix`.
    /// property `row-prefix`.
    /// property `row-prefix`.
    /// property `row-prefix`.
    in property <string> row-prefix;

    height: Spacing.xl;
    border-radius: Spacing.xs;
    background: Colors.bg-elevated;
    border-width: Sizes.border-w;
    border-color: Colors.border;

    Text {
        x: Spacing.sm;
        text: root.row-prefix + root.row-index;
        font-size: Type.body-size;
        color: Colors.text-primary;
        vertical-alignment: center;
    }
}
