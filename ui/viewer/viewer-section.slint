// ViewerSection — titled section divider for Framework Viewer pages.
// Used by all page-*.slint files: pass title + children.

import { Colors, Spacing, Type } from "../globals/theme.slint";
import { Sizes }                 from "../state/sizes.slint";

/// V ie we rs ec ti on component.
/// component `ViewerSection`.
export component ViewerSection inherits VerticalLayout {
    /// Input property "title".
    /// property `title`.
    in property <string> title;
    spacing: Spacing.sm;
    padding: Spacing.md;

    Text {
        text: root.title;
        font-size: Type.subtitle-size;
        font-weight: Type.weight-semibold;
        color: Colors.text-primary;
    }

    Rectangle {
        height: Sizes.border-w;
        background: Colors.border;
    }

    @children
}
