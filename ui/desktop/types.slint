/// N av it em struct.
/// struct `NavItem`.
export struct NavItem {
    id:        string,
    label:     string,
    icon:      string,
    is-header: bool,   // true = kollapsibel sektions-header
    hidden:    bool,   // true = skjult (gruppe er kollapset)
}
