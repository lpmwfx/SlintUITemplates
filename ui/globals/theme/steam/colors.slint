// Steam dark-first color palette — shared by steam-linux and steam-deck.
// No light mode. Based on Steam Store / Big Picture dark UI.

export global SteamColors {
    /// Background — Steam dark navy.
    /// property `bg-primary`.
    /// property `bg-primary`.
    /// property `bg-primary`.
    /// property `bg-primary`.
    /// property `bg-primary`.
    /// property `bg-primary`.
    /// property `bg-primary`.
    out property <color> bg-primary:      #1b2838;
    /// Surface — Steam blue-grey card background.
    /// property `bg-surface`.
    /// property `bg-surface`.
    /// property `bg-surface`.
    /// property `bg-surface`.
    /// property `bg-surface`.
    /// property `bg-surface`.
    /// property `bg-surface`.
    out property <color> bg-surface:      #2a475e;
    /// Elevated surface — slightly lighter card.
    /// property `bg-elevated`.
    /// property `bg-elevated`.
    /// property `bg-elevated`.
    /// property `bg-elevated`.
    /// property `bg-elevated`.
    /// property `bg-elevated`.
    /// property `bg-elevated`.
    out property <color> bg-elevated:     #1e3448;
    /// Primary text — Steam light grey.
    /// property `text-primary`.
    /// property `text-primary`.
    /// property `text-primary`.
    /// property `text-primary`.
    /// property `text-primary`.
    /// property `text-primary`.
    /// property `text-primary`.
    out property <color> text-primary:    #c6d4df;
    /// Secondary text — Steam muted grey.
    /// property `text-secondary`.
    /// property `text-secondary`.
    /// property `text-secondary`.
    /// property `text-secondary`.
    /// property `text-secondary`.
    /// property `text-secondary`.
    /// property `text-secondary`.
    out property <color> text-secondary:  #8f98a0;
    /// Disabled text.
    /// property `text-disabled`.
    /// property `text-disabled`.
    /// property `text-disabled`.
    /// property `text-disabled`.
    /// property `text-disabled`.
    /// property `text-disabled`.
    /// property `text-disabled`.
    out property <color> text-disabled:   #4b6070;
    /// Accent — Steam blue.
    /// property `accent`.
    /// property `accent`.
    /// property `accent`.
    /// property `accent`.
    /// property `accent`.
    /// property `accent`.
    /// property `accent`.
    out property <color> accent:          #1a9fff;
    /// Accent hover state.
    /// property `accent-hover`.
    /// property `accent-hover`.
    /// property `accent-hover`.
    /// property `accent-hover`.
    /// property `accent-hover`.
    /// property `accent-hover`.
    /// property `accent-hover`.
    out property <color> accent-hover:    #2196f3;
    /// Text on accent background.
    /// property `accent-text`.
    /// property `accent-text`.
    /// property `accent-text`.
    /// property `accent-text`.
    /// property `accent-text`.
    /// property `accent-text`.
    /// property `accent-text`.
    out property <color> accent-text:     #ffffff;
    /// Default border.
    /// property `border`.
    /// property `border`.
    /// property `border`.
    /// property `border`.
    /// property `border`.
    /// property `border`.
    /// property `border`.
    out property <color> border:          #3d6b8a;
    /// Strong border — focus rings, active states.
    /// property `border-strong`.
    /// property `border-strong`.
    /// property `border-strong`.
    /// property `border-strong`.
    /// property `border-strong`.
    /// property `border-strong`.
    /// property `border-strong`.
    out property <color> border-strong:   #5c8eae;
    /// Drop shadow color.
    /// property `shadow`.
    /// property `shadow`.
    /// property `shadow`.
    /// property `shadow`.
    /// property `shadow`.
    /// property `shadow`.
    /// property `shadow`.
    out property <color> shadow:          #00000099;
    /// Medium overlay — dialogs, drawers.
    /// property `overlay-medium`.
    /// property `overlay-medium`.
    /// property `overlay-medium`.
    /// property `overlay-medium`.
    /// property `overlay-medium`.
    /// property `overlay-medium`.
    /// property `overlay-medium`.
    out property <color> overlay-medium:  #00000066;
    /// Light overlay — hover states.
    /// property `overlay-light`.
    /// property `overlay-light`.
    /// property `overlay-light`.
    /// property `overlay-light`.
    /// property `overlay-light`.
    /// property `overlay-light`.
    /// property `overlay-light`.
    out property <color> overlay-light:   #00000040;
}
