// Material 3 spacing — 8dp base grid.
// Reference: https://m3.material.io/foundations/layout/understanding-layout/spacing

/// M at er ia ls pa ci ng global values.
export global MaterialSpacing {
    /// Output property "xs".
    /// property `xs`.
    /// property `xs`.
    /// property `xs`.
    /// property `xs`.
    /// property `xs`.
    /// property `xs`.
    /// property `xs`.
    out property <length> xs:   4px;
    /// Output property "sm".
    /// property `sm`.
    /// property `sm`.
    /// property `sm`.
    /// property `sm`.
    /// property `sm`.
    /// property `sm`.
    /// property `sm`.
    out property <length> sm:   8px;
    /// Output property "md".
    /// property `md`.
    /// property `md`.
    /// property `md`.
    /// property `md`.
    /// property `md`.
    /// property `md`.
    /// property `md`.
    out property <length> md:  16px;
    /// Output property "lg".
    /// property `lg`.
    /// property `lg`.
    /// property `lg`.
    /// property `lg`.
    /// property `lg`.
    /// property `lg`.
    /// property `lg`.
    out property <length> lg:  24px;
    /// Output property "xl".
    /// property `xl`.
    /// property `xl`.
    /// property `xl`.
    /// property `xl`.
    /// property `xl`.
    /// property `xl`.
    /// property `xl`.
    out property <length> xl:  32px;

    // ── Component heights ─────────────────────────────────────────────────
    /// Output property "top-app-bar-height".
    /// property `top-app-bar-height`.
    /// property `top-app-bar-height`.
    /// property `top-app-bar-height`.
    /// property `top-app-bar-height`.
    /// property `top-app-bar-height`.
    /// property `top-app-bar-height`.
    /// property `top-app-bar-height`.
    out property <length> top-app-bar-height:    64px;
    /// Output property "bottom-nav-bar-height".
    /// property `bottom-nav-bar-height`.
    /// property `bottom-nav-bar-height`.
    /// property `bottom-nav-bar-height`.
    /// property `bottom-nav-bar-height`.
    /// property `bottom-nav-bar-height`.
    /// property `bottom-nav-bar-height`.
    /// property `bottom-nav-bar-height`.
    out property <length> bottom-nav-bar-height: 80px;
    /// Output property "nav-item-height".
    /// property `nav-item-height`.
    /// property `nav-item-height`.
    /// property `nav-item-height`.
    /// property `nav-item-height`.
    /// property `nav-item-height`.
    /// property `nav-item-height`.
    /// property `nav-item-height`.
    out property <length> nav-item-height:       64px;
    /// Output property "fab-size".
    /// property `fab-size`.
    /// property `fab-size`.
    /// property `fab-size`.
    /// property `fab-size`.
    /// property `fab-size`.
    /// property `fab-size`.
    /// property `fab-size`.
    out property <length> fab-size:              56px;
    /// Output property "fab-extended-height".
    /// property `fab-extended-height`.
    /// property `fab-extended-height`.
    /// property `fab-extended-height`.
    /// property `fab-extended-height`.
    /// property `fab-extended-height`.
    /// property `fab-extended-height`.
    /// property `fab-extended-height`.
    out property <length> fab-extended-height:   56px;
    /// Output property "icon-button-size".
    /// property `icon-button-size`.
    /// property `icon-button-size`.
    /// property `icon-button-size`.
    /// property `icon-button-size`.
    /// property `icon-button-size`.
    /// property `icon-button-size`.
    /// property `icon-button-size`.
    out property <length> icon-button-size:      48px;

    // ── Control heights (M3 interactive touch targets) ────────────────────
    /// Output property "control-sm".
    /// property `control-sm`.
    /// property `control-sm`.
    /// property `control-sm`.
    /// property `control-sm`.
    /// property `control-sm`.
    /// property `control-sm`.
    /// property `control-sm`.
    out property <length> control-sm:  40px;   // compact touch target
    /// Output property "control-md".
    /// property `control-md`.
    /// property `control-md`.
    /// property `control-md`.
    /// property `control-md`.
    /// property `control-md`.
    /// property `control-md`.
    /// property `control-md`.
    out property <length> control-md:  48px;   // standard M3 minimum touch target
    /// Output property "control-lg".
    /// property `control-lg`.
    /// property `control-lg`.
    /// property `control-lg`.
    /// property `control-lg`.
    /// property `control-lg`.
    /// property `control-lg`.
    /// property `control-lg`.
    out property <length> control-lg:  56px;   // large / primary (FAB height)

    // ── Nav chrome ────────────────────────────────────────────────────────
    /// Output property "nav-rail-width".
    /// property `nav-rail-width`.
    /// property `nav-rail-width`.
    /// property `nav-rail-width`.
    /// property `nav-rail-width`.
    /// property `nav-rail-width`.
    /// property `nav-rail-width`.
    /// property `nav-rail-width`.
    out property <length> nav-rail-width: 80px;  // M3 Navigation Rail width
}
