/// S pa ci ng global values.
export global Spacing {
    // ── Existing scale (used by components for container sizing) ──────────
    /// Output property "xs".
    /// property `xs`.
    /// property `xs`.
    /// property `xs`.
    /// property `xs`.
    /// property `xs`.
    /// property `xs`.
    /// property `xs`.
    out property <length> xs:  4px;
    /// Output property "sm".
    /// property `sm`.
    /// property `sm`.
    /// property `sm`.
    /// property `sm`.
    /// property `sm`.
    /// property `sm`.
    /// property `sm`.
    out property <length> sm:  8px;
    /// Output property "md".
    /// property `md`.
    /// property `md`.
    /// property `md`.
    /// property `md`.
    /// property `md`.
    /// property `md`.
    /// property `md`.
    out property <length> md:  16px;
    /// Output property "lg".
    /// property `lg`.
    /// property `lg`.
    /// property `lg`.
    /// property `lg`.
    /// property `lg`.
    /// property `lg`.
    /// property `lg`.
    out property <length> lg:  24px;
    /// Output property "xl".
    /// property `xl`.
    /// property `xl`.
    /// property `xl`.
    /// property `xl`.
    /// property `xl`.
    /// property `xl`.
    /// property `xl`.
    out property <length> xl:  48px;   // component container height (nav rows, toolbar)

    // ── Fluent 2 control heights ──────────────────────────────────────────
    /// Output property "control-sm".
    /// property `control-sm`.
    /// property `control-sm`.
    /// property `control-sm`.
    /// property `control-sm`.
    /// property `control-sm`.
    /// property `control-sm`.
    /// property `control-sm`.
    out property <length> control-sm: 24px;
    /// Output property "control-md".
    /// property `control-md`.
    /// property `control-md`.
    /// property `control-md`.
    /// property `control-md`.
    /// property `control-md`.
    /// property `control-md`.
    /// property `control-md`.
    out property <length> control-md: 32px;
    /// Output property "control-lg".
    /// property `control-lg`.
    /// property `control-lg`.
    /// property `control-lg`.
    /// property `control-lg`.
    /// property `control-lg`.
    /// property `control-lg`.
    /// property `control-lg`.
    out property <length> control-lg: 40px;

    // ── Chrome ────────────────────────────────────────────────────────────
    /// Output property "handle-thickness".
    /// property `handle-thickness`.
    /// property `handle-thickness`.
    /// property `handle-thickness`.
    /// property `handle-thickness`.
    /// property `handle-thickness`.
    /// property `handle-thickness`.
    /// property `handle-thickness`.
    out property <length> handle-thickness: 5px;
    /// Output property "handle-hit".
    /// property `handle-hit`.
    /// property `handle-hit`.
    /// property `handle-hit`.
    /// property `handle-hit`.
    /// property `handle-hit`.
    /// property `handle-hit`.
    /// property `handle-hit`.
    out property <length> handle-hit:       12px;

    // ── Utility ───────────────────────────────────────────────────────────
    /// Output property "border".
    /// property `border`.
    /// property `border`.
    /// property `border`.
    /// property `border`.
    /// property `border`.
    /// property `border`.
    /// property `border`.
    out property <length> border: 1px;   // standard 1px divider / border
    /// Output property "none".
    /// property `none`.
    /// property `none`.
    /// property `none`.
    /// property `none`.
    /// property `none`.
    /// property `none`.
    /// property `none`.
    out property <length> none:   0px;   // zero-length (collapsed state)
}
